// 如何定义 JavaScript 模块
// 在 TypeScript 中，就像在 ECMAScript 2015 中一样，任何包含顶级或的文件都被视为一个模块。importexport
// 相反，没有任何顶级导入或导出声明的文件被视为脚本，其内容在全局范围内可用（因此也可用于模块）。
// 模块在其自己的范围内执行，而不是在全局范围内执行。这意味着在模块中声明的变量、函数、类等在模块外部不可见，
// 除非使用其中一个导出窗体显式导出它们。相反，要使用从其他模块导出的变量、函数、类、接口等，必须使用其中一个导入表单进行导入。



